# `.planning/todos/` — DEPRECATED

> **状态:** 已弃用,不再作为活任务入口。
> **弃用日期:** 2026-07-03
> **依据:** `_meta/policies/BACKLOG_INTAKE_POLICY.md` §2(权威边界)

## 为什么弃用

本目录曾用于 GSD `gsd-add-todo` / `gsd-check-todos` 的 `pending/ + completed/` 约定,但实际只沉淀了少量自由文本反思笔记(非结构化 chore),已停更两个月以上,且与 portfolio 的其他 backlog 载体职责重叠。

按 BACKLOG INTAKE POLICY,可执行待办的权威载体已明确为:

- **可执行待办** → 对应 repo 的 **GitHub Issues**(带准入标签)
- **phase / spec / track 规划** → `.planning/<track>/`
- **portfolio 级排序** → `agentic-evolution/ROADMAP.md`

本目录不属于以上任何一层,故弃用。

## 残留内容处置

`pending/` 下 2 条 `2026-04-26-cat-wu-*` 是**设计/反思文档,不是 chore 条目**,因此**保留原地归档**(不迁为 issue)。如需引用,按普通归档文档对待。

## 不要做

- 不要在此目录新增待办文件。
- 新可执行待办 → 去对应 repo 开 GitHub Issue 并按 policy 打准入标签。
