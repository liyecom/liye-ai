# FIXTURE (expect: FAIL on C11a) — a kill_switch that CLAIMS tested:true but binds
# no drill evidence (test_evidence_ref / test_evidence_hash omitted). "We tested it"
# is exactly the class of self-declared claim this contract exists to reject: tested
# is free to say and expensive to prove, so the proof is what the schema requires.
# tested:false would be honest and valid here; tested:true without evidence is not.
# Attended + C10-satisfied so C11a is the sole violation.
contract_kind: governed_work_loop
schema_version: "2.0.0"
contract_status: schema_validated_only
instance_scope: template

goal:
  statement: "修复 test/auth 下失败测试，保持 src/auth public API 不变。"
  lifecycle: active
  success_criteria:
    - id: SC1
      statement: "test/auth 全绿。"

scope:
  scope_roots: ["src/auth", "test/auth"]
  no_mutation: false
  control_plane_touch: false
  allowed_actions: [read_file, edit_file, run_tests]

invariants:
  hard: []
  review: []

evidence:
  authoritative:
    - check:
        command: "npm test -- test/auth"
      expected:
        kind: exit_code
        value: 0
      artifact_ref: "state/runtime/loop/auth/test_result.json"

checker:
  independence_level: deterministic
  hard_gate_grading: deterministic_only
  on_checker_error: fail_closed
  readable_artifacts: ["the diff", "test output"]
  forbids_reading_maker_reasoning: true

runtime:
  mode: attended
  scheduler_owner: claude_goal

budget:
  limits:
    turns: 20
  on_exhaustion: stop_new_mutations_then_wrap_up

stop_condition:
  success_check:
    kind: deterministic_predicate
    check_ref: "npm test -- test/auth"
  max_iterations: 10
  on_max_iterations: stop_inconclusive_then_wrap_up

kill_switch:
  mechanism: env_gate
  location: "LOOP_AUTH_FIX_ENABLED"
  polarity: enable_required
  halt_semantics: no_new_iterations_then_wrap_up
  tested: true
  # test_evidence_ref / test_evidence_hash OMITTED  <-- VIOLATES C11a: tested:true
  # is a claim; the drill artifact (ref + sha256) is what makes it a fact.

evidence_package:
  required_contents: [contract_snapshot, iteration_log, evidence_items, final_verdict, wrap_up]
  package_ref: "state/runtime/loop/auth/package/"

idempotency:
  replay_safe: true
  key_strategy: per_action_dedupe_key

control_plane_mutation: none

next_action_card:
  enum: [continue, ask_human, stop_success, stop_inconclusive]
  maps_to_existing_status:
    continue: "TaskStatus.EXECUTING | VerdictDecision.EXPAND"
    ask_human: "TaskStatus.AWAITING_APPROVAL | NextStep.REQUEST_APPROVAL"
    stop_success: "TaskStatus.COMPLETED | VerdictDecision.PASS"
    stop_inconclusive: "VerdictDecision.HOLD | HoldReason.INSUFFICIENT_EVIDENCE"
    stop_dormant: "TaskStatus.BLOCKED | NextStep.RETRY_LATER"
    escalate_live_authorization: "NextStep.ENABLE_WRITES | NextStep.CLEAR_KILL_SWITCH"

wrap_up:
  done: []
  undone: []
  failed_commands: []
  assumptions: []
  next_steps: []
