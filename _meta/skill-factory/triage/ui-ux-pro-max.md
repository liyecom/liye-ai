# Skill triage record — `ui-ux-pro-max`

**Status**: `QUARANTINE — pending Builder-Contract subsystem decision`
**Skill**: `Skills/00_Core_Utilities/creative/ui-ux-pro-max/`
**Recon**: 2026-07-09 · read-only live recon (zero writes to the skill / registry / vendored copy / tracks/)
**Dispatch source**: `_meta/skill-factory/SKILL_FACTORY_CONTRACT_v0.2.md` §127-128（delete-or-authoring 规则 + 点名示例）

---

## 判决（authoritative verdict）

- **QUARANTINE, not deletion**
- **not eligible for v0.2 lift until Builder-Contract is revived**
- **not exempted as vendored**
- **not counted as authored v0.2 backlog until owner decides subsystem direction**
- **delete/revive requires subsystem-level decision, not isolated skill cleanup**

这不是 skill 层能单独裁决的问题。真实铰链是 `Builder-Contract / tracks/` 子系统是否回到 go-forward 架构；直接拍「复活」会把一个 6 个月静止的子系统重新纳入治理承诺，直接拍「删除」又等于绕过子系统级退役评审。两者都超出本次 recon 的证据边界。本记录只把裁决入口钉牢，不做产品裁决。

## 这条记录要堵住的三个误判

未来 agent 面对本 skill 时最容易踩的三件事，逐一封死：

1. **「看到 stub 就删」** —— 它不是 `amazon-ad-optimization` 式死空壳。是连贯的自写 translator（205 行、单一职责、deps 齐全、有产出足迹）。删它必须走子系统级退役，不是孤立 cleanup。
2. **「看到自写就 lift」** —— 它确是自写，但其唯一目的子系统（`tracks/ Builder-Contract`）休眠、零 runtime、零 CI 消费。现在 lift 到 v0.2 = 给死水镀金。Builder-Contract 复活前不 lift。
3. **「看到同名 vendored 就乱同步」** —— AGE `vendor/liye-ai/.../ui-ux-pro-max` 与本件已漂移（见下），且 sibling `development-tools/ui-ux`（外部 fork，upstream 名 `ui-ux-pro-max-skill`）与本件**同源撞名但不同物**。任何「同步/统一」动作须先分清三者，勿盲动。

## Recon 证据（六维，一手，2026-07-09）

| 维度 | 判定 | 证据锚 |
|---|---|---|
| 真·自写? | 是 | `.planning/todos/pending/2026-04-26-cat-wu-four-direction-reflection.md` D-4：「the one true user-authored skill」；非 fork |
| 引用基础设施存在? | 全存在，无 dangling | `knowledge/design/ui-ux/` · `_meta/governance/validator.py` · `SKILL_CONSTITUTION_v0.1.md` · `_meta/contracts/site-design.contract.yaml`（inherit-from）· `_meta/schemas/contracts.schema.json` 逐个 EXISTS |
| 有产出足迹? | 有，1 次 | `tracks/example-saas-dashboard/site-design.contract.yaml` 头注「Generated by: ui-ux-pro-max Skill」 |
| triggerable? | 否 | `/ui-ux-pro-max` 无任何 runtime 派发；skill 运行时在 kernel 静止区 |
| 注册在 registry? | 否 | `00_Core_Utilities` 无 `index.yaml`；`.claude/skills/index.yaml` 未列；无 domain index 列它 |
| 所属子系统还活? | 否，~6 个月冻结 | `tracks/` 最近 commit `72e0867`（2026-01-13，= 建 skill 的同一 commit）；无 workflow 消费 `site-design.contract`；Builder-Contract 管线自 1 月未动 |

git 历史：仅 2 次提交，`72e0867`(2026-01-13 建) → `7481503`(2026-01-16 SFC v0.1 迁移)，此后零改动 = created-then-frozen。

## 对 SFC §126-128 刻画的精化（不改契约正文，仅在此记录）

契约 §126 将本件归入「既无 upstream 标记、又无真·LiYe 治理内容的机器脚手架空壳」示例。一手 recon 部分**修正**该刻画：本件**有**真·LiYe 治理内容（连贯 translator body + 真实 deps + 产出足迹），并非空壳。准确定性是 **「连贯但休眠的自写 skill，其目的子系统亦休眠」**。契约的 delete-or-authoring 处置框架仍适用，只是「删（死代码）」这一支被证据下调优先级——见判决。

§127-128 复核两条 SFC 指控：
- `outputs: ["SKILL.md"]` **确为 scaffold-default 缺陷**（body 明写唯一产出是 `site-design.contract.yaml`，自相矛盾）——但**不在本 PR 修**（QUARANTINE，复活时再修）。
- 「残缺 description」**已不成立**：现 description「Translate human UI/UX requirements into site-design.contract.yaml」完整可用。

## Owner 决策铰链（recon 无法从代码判定，留待翻牌）

**问题**：`tracks/ Builder-Contract / site-design.contract` 子系统是否在 go-forward 架构里？

- **若 revive Builder-Contract** → 本 skill 是现成 translator 入口，届时进入 v0.2 authoring（顺带修 `outputs` + 重接 registry/trigger）。
- **若 retire Builder-Contract** → 本 skill 随子系统一并退役（单独 RFC / 子系统级评审），而非孤立删除。

在此之前：维持 QUARANTINE，不删、不 lift、不接 registry/trigger、不动 vendored 副本、不修 `outputs`。

## 次要发现（mention-don't-fix，不在本 PR 处理）

1. **vendored AGE 副本已漂移**：`liye_os` 原件 `SKILL.md` sha256 `81139613f4a280ec4796547206485a29d9ee1eaafd07c4b75cda6d726f062f38` ≠ AGE `vendor/liye-ai/Skills/00_Core_Utilities/creative/ui-ux-pro-max/SKILL.md` sha256 `a8b121ddf3d41bbc1878ac4e0e308455f015baf0fbc9930b62efff093c77f470`（template 一致，仅 `SKILL.md` 漂移）。AGE 侧亦无 runtime consumer（仅 `vendored-skills.lock.json` + sweep report + patch 脚本 NAME 它）。skill 去留定了，vendored 副本 + lockfile 条目应随之（留则 re-sync，删则一并清）——**不在本 recon scope**。
2. **命名撞名**是长期 discoverability 风险：自写 `ui-ux-pro-max`、fork `ui-ux`（upstream `ui-ux-pro-max-skill`）三名交叠，易误判依赖关系。
