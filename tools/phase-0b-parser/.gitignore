__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/
dist/
build/
*.egg-info/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# M6 — default sealed-registry output dir (per SPEC §6.4 whitelist).
# The artifact is regenerated by report_sealed_registry per run; never
# commit because each run produces redacted token references that should
# be derived from a fresh portfolio scan.
var/

# Un-ignore test fixtures (these contain *mock* tokens by design — pattern shape
# matches sk_/pk_/jwt regex but values are deliberately fake "F1MOCK..." etc.).
# Root .gitignore globs .env / .env.local / **/.env etc., but the parser test
# suite needs these files committed so CI can reproduce M2 acceptance.
!tests/fixtures/
!tests/fixtures/**

